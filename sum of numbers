int sum(int n);
int main()
{ 
    int n,s ;
    printf(" enter the number you want to get sum: ");
    scanf("%d", &n);
	s= sum(n);
	printf("The sum of %d is : %d",n,s);
}
int sum(int n)
{
	
	if(n==0)
	return 0;
	else 
	return((n%10)+sum(n/10));	
}
